export type PmList = {
  id: number;
  name: string;
  url: string;
};

export type PmUI = {
  id: number;
  name: string;
  types: string[];
  forms: string[];
  abilities: string[];
  img: string;
  species: string;
};
