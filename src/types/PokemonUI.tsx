export type PmList = {
  id: number;
  name: string;
  url: string;
};

export type PmUI = {
  id: number;
  name: string;
  types: string[];
  abilities: string[];
  img: string;
  height: number;
  weight: number;
};
